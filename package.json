{
  "name": "probot-metadata",
  "version": "2.0.0",
  "description": "A Probot extension to store metadata on Issues and Pull Requests",
  "main": "index.js",
  "scripts": {
    "test": "PRIVATE_KEY_PATH=test/test-key.pem jest && standard",
    "test:watch": "PRIVATE_KEY_PATH=test/test-key.pem jest --watch --notify --notifyMode=change --coverage"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/probot/metadata.git"
  },
  "keywords": [
    "probot"
  ],
  "author": "Brandon Keepers",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/probot/metadata/issues"
  },
  "homepage": "https://github.com/probot/metadata#readme",
  "devDependencies": {
    "jest": "^23.6.0",
    "probot": "^0.11.0",
    "standard": "^10.0.3"
  },
  "standard": {
    "env": "jest"
  },
  "dependencies": {
    "node-jose": "^1.0.0",
    "rsa-pem-to-jwk": "^1.1.3"
  },
  "jest": {
    "testEnvironment": "node"
  }
}
